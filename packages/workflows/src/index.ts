// import { Workflow as WorkflowType } from '@srclaunch/types';

// export class WorkflowJob {
//   constructor(steps?: Array<WorkflowStep | { [name: string]: WorkflowStep }>) {
//     this.steps = steps || [];
//   }
// }

export * from './project/index';

// export class Workflow implements WorkflowType<unknown> {}
