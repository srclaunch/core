// export async function generateReactComponent({
//   name,
//   path,
// }: ReactComponentGeneratorOptions): Promise<void> {
//   const componentPath = path ?? name;
//   const componentFileName = `${name}.tsx`;
//   const componentFilePath = join(componentPath, componentFileName);
//   const componentFile = await fs.readFile
export async function generateReactComponent({
  name,
  path,
}: {
  readonly name: any;
  readonly path: any;
}) {}
