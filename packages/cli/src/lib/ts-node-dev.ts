// import path from 'path';
// @ts-ignore
// import { runDev } from 'ts-node-dev';
// import { parentPort, workerData } from 'worker_threads';

// const moduleURL = new URL(import.meta.url);
// const __dirname = path.dirname(moduleURL.origin);

// const appPath = workerData.app
//   ? `${__dirname}/apps/${workerData.app}/src/index.ts`
//   : null;

// if (appPath) {
//   const process = await runDev(appPath, [], [], []);

//   if (parentPort) parentPort.postMessage({ message: process });
// }

// setInterval(() => {

// }, 1000);

export {};
