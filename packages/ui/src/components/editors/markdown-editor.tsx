import { memo, ReactElement } from 'react';

// type MarkdownEditorProps = {
//   readonly onChange: (value: string) => void;
//   readonly value: string;
// };

export const MarkdownEditor = memo((): ReactElement => {
  return <div />;
});
