import { memo, ReactElement } from 'react';

import { Container } from '../../../layout/container';

// type PricingPlansProps = ContainerProps;

export const PricingPlans = memo((): ReactElement => {
  return <Container>Pricing plans</Container>;
});
