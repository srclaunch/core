import { memo, ReactElement } from 'react';

// import { InputProps } from '../shared/input';

// type TagInputProps = InputProps<string | number | boolean>;

export const TagInput = memo((): ReactElement => {
  return <div></div>;
});
