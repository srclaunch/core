import { CSSMeasurement } from '../../common';

export type PaddingProps = {
  readonly padding?: CSSMeasurement;
  readonly paddingBottom?: CSSMeasurement;
  readonly paddingLeft?: CSSMeasurement;
  readonly paddingRight?: CSSMeasurement;
  readonly paddingTop?: CSSMeasurement;
};
