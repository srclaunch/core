import { CSSMeasurement } from '../../common';

export type MarginProps = {
  readonly margin?: CSSMeasurement;
  readonly marginBottom?: CSSMeasurement;
  readonly marginLeft?: CSSMeasurement;
  readonly marginRight?: CSSMeasurement;
  readonly marginTop?: CSSMeasurement;
};
