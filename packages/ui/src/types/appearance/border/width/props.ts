import { CSSMeasurement } from '../../../common';

export type BorderWidthProps = {
  readonly borderBottomWidth?: CSSMeasurement;
  readonly borderLeftWidth?: CSSMeasurement;
  readonly borderRightWidth?: CSSMeasurement;
  readonly borderTopWidth?: CSSMeasurement;
  readonly borderWidth?: CSSMeasurement;
};
