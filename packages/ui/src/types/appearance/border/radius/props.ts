import { CSSMeasurement } from '../../../common';

export type BorderRadiusProps = {
  readonly borderRadius?: CSSMeasurement;
  readonly borderRadiusBottomLeft?: CSSMeasurement;
  readonly borderRadiusBottomRight?: CSSMeasurement;
  readonly borderRadiusTopLeft?: CSSMeasurement;
  readonly borderRadiusTopRight?: CSSMeasurement;
};
